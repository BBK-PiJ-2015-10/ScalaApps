import akka.actor.{Actor,ActorRef,Props}

class Master(nrOfWorker: Int, nrOfMessage: Int, nrOfElements: Int, listener: ActorRef) extends Actor {
  
  var pi: Double = _
  
  var nrOfResults: Int = _
  
  val start: Long = System.currentTimeMillis
  
  
  
}